server:
  port: 9000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zmblog?serverTimezone=Asia/Shanghai&useUnicode=true&useSSL=false&characterEncoding=UTF8
    username: root
    password: ZMMMMMMM191010
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true
  thymeleaf:
    cache: false
  # redis和redis session
  redis:
    port: 6379
    database: 0
    host: localhost

logging:
  level:
    root: info
    com: debug
  file:
    name: log/blog-dev.log