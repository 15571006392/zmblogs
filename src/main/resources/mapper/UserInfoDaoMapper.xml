<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserInfoDao">

    <select id="findUserById" resultType="UserInfo">
        select u.id,u.avatar,u.create_time,u.update_time,u.email,u.nickname,count(d.id) as blog_count from t_user u join t_detail d on u.id = d.user_id where u.id = #{id}
    </select>

    <select id="findUserDetail" resultType="Detail">
        select id,title,views from t_detail where user_id = #{id} order by views desc limit 8
    </select>

    <update id="updateUserUpdateTime">
        update t_user set update_time = #{updateTime} where id = #{id}
    </update>

</mapper>