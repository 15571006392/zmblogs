<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserInfoDao">

    <select id="findUserById" resultType="UserInfo">
        select id,avatar,create_time,email,nickname from t_user where id = #{id}
    </select>

    <select id="findUserDetail" resultType="Detail">
        select id,title,views from t_detail where user_id = #{id} order by views desc limit 5
    </select>

</mapper>